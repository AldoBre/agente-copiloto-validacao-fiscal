# ============================================================
# Agente de Implantação Fiscal — Senior ERP
# Python 3.11+ (testado em 3.12)
# ============================================================

# --- Core web ---------------------------------------------------------------
Django>=5.1,<5.3
djangorestframework>=3.15
django-cors-headers>=4.6
python-dotenv>=1.0
whitenoise>=6.8

# --- Camada de API assíncrona (streaming do chat) ---------------------------
fastapi>=0.115
uvicorn[standard]>=0.34
python-multipart>=0.0.20

# --- IA / Agentes -----------------------------------------------------------
langchain>=0.3.14
langchain-core>=0.3.29
langchain-community>=0.3.14
langchain-text-splitters>=0.3.5
langgraph>=0.2.60
langgraph-checkpoint-sqlite>=2.0.1

# --- Conectores de provedores de IA (todos opcionais em runtime) ------------
# O backend importa sob demanda: instale só os que for usar.
langchain-anthropic>=0.3.0
langchain-openai>=0.3.0
langchain-google-genai>=2.0.0
langchain-groq>=0.2.0
langchain-mistralai>=0.2.0
langchain-ollama>=0.2.0
langchain-deepseek>=0.1.2

# --- RAG / Base de conhecimento --------------------------------------------
numpy>=2.0
tiktoken>=0.8
# Stemming português do BM25 (puro Python, sem binário)
snowballstemmer>=2.2

# --- Scraping da documentação Senior ---------------------------------------
httpx>=0.28
beautifulsoup4>=4.12
lxml>=5.3
markdownify>=0.14

# --- Tabelas fiscais oficiais -----------------------------------------------
# TIPI, CFOP e FCP sao publicados em XLSX. Sem isto a carga dessas tres falha
# e o validador fica sem base para conferir IPI, CFOP e FCP.
openpyxl>=3.1

# --- Segurança / utilitários ------------------------------------------------
cryptography>=44.0
pydantic>=2.10
python-dateutil>=2.9

# Busca vetorial no proprio PostgreSQL: o embedding vira coluna `vector` e a
# similaridade roda no banco, em vez de carregar a base toda na RAM do app.
pgvector>=0.3

# --- Banco: PostgreSQL ------------------------------------------------------
# Banco oficial do projeto e padrão (SQLite continua disponível para rodar
# sem infra: DB_ENGINE=sqlite).
psycopg[binary]>=3.2
